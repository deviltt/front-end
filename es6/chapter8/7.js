let colors=[5,4,3,2,1];

for(let entry of colors.entries()){
    console.log(entry);
}

for (let key of colors.keys()){
    console.log(key);
}

for(let value of colors.values()){
    console.log(value);
}

for (let val of colors){
    console.log(val);
} 
